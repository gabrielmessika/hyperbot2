[project]
name = "hyperbot2"
version = "0.2.0"
description = "Research-first market-making bot for Hyperliquid"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "websockets>=15.0",
]

[project.scripts]
hyperbot2 = "hyperbot2.cli:main"

[dependency-groups]
dev = [
  "mypy>=1.14",
  "pytest>=8.3",
  "pytest-cov>=6.0",
  "ruff>=0.9",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/hyperbot2"]

[tool.pytest.ini_options]
addopts = "-ra --strict-config --strict-markers"
testpaths = ["tests"]

[tool.ruff]
line-length = 88
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["hyperbot2"]
mypy_path = "src"
